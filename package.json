{
  "name": "gitifact-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": "10.33.0"
  },
  "scripts": {
    "build": "pnpm --filter @gitifact/core --filter @gitifact/contracts build && pnpm --filter @gitifact/browser build && pnpm --filter gitifact build",
    "typecheck": "pnpm --filter @gitifact/core --filter @gitifact/contracts build && pnpm -r typecheck",
    "dev": "pnpm --filter @gitifact/browser dev",
    "dev:cli": "pnpm --filter @gitifact/core --filter @gitifact/contracts build && pnpm --filter @gitifact/browser build && pnpm --parallel --filter @gitifact/core --filter @gitifact/contracts --filter gitifact dev",
    "cli": "node apps/cli/dist/main.js",
    "test": "pnpm build && pnpm test:built",
    "test:built": "pnpm -r --workspace-concurrency=1 --if-present test:built && node scripts/test-package.mjs",
    "test:package": "node scripts/test-package.mjs",
    "check": "pnpm typecheck && pnpm test",
    "browser:install": "pnpm --filter @gitifact/browser exec playwright install chromium"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "24.13.4"
  }
}
