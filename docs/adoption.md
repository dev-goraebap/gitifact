# Tryce 프로젝트 자체 도입

2026-09-13 사용자가 도입 프롬프트 정리, 기존 프로젝트 요구사항 도출, 이 저장소의 자동모드 전환과 Tryce 커밋 적용을 요청했다. 지정된 CLI 0.2.1을 사용한다. 기존 prototype-1 설정은 CLI가 백업하고 도입 기준선과 note는 그대로 유지했다. 과거 커밋은 재작성하지 않는다.

## 조사와 등록 범위

사용자 대화, 제품 기준, 기록 정책·요구사항 수집·실행 형식·스킬 배포·세션 브리핑·브라우저 명세, 관련 CLI 명령과 테스트를 함께 확인했다. 문서가 있다는 사실만으로 구현됐다고 판단하지 않았다. 이력의 기준점은 자체 도입 직전 커밋 `3a83e5c`이며 현재 요청으로 추가한 지침 연결·도출 절차는 해당 요청을 근거로 구분했다.

첫 등록은 제품 원칙, 에이전트 도입, 기록 정책, Git 연결, 조회의 주요 조건이다. 모든 과거 문서의 세부 규약이나 예외를 일괄 복제한 목록은 아니다. 배열 상한·HTTP 응답별 조건·아키텍처 세부 선택 등은 기존 명세를 유지하고 해당 영역을 변경할 때 필요에 따라 편입한다. 이슈 트래커 연계는 개발 여부가 미정이므로 요구사항으로 확정하지 않았다. GUI와 전체 이력 검사는 방향만 알려져 있어 구체적인 수용 조건을 정할 초안으로 남긴다.

명확한 사용자 의도와 근거가 일치한 항목은 자동모드에서 Codex가 확정한다. 사용자 승인으로 표시하지 않으며 모든 항목의 구현·검증 상태는 CLI의 `not-assessed`·`not-run`을 유지한다. 실제 실행 검증 결과는 요구사항 활성 상태와 별도로 기록한다.

## 지침 연결

AGENTS.md가 이미 공용 스킬을 연결하고 CLAUDE.md가 AGENTS.md를 읽도록 안내한다. 이 참조를 유지하며 중복 안내나 새로운 CLAUDE.md를 만들지 않았다. README 도입 프롬프트는 지침 파일의 유무와 기존 참조를 확인하고 필요한 연결만 추가하도록 한다. CLI에 지침 파일 생성·동기화 기능을 추가한 것은 아니다.

## 개발 예외와 검증 범위

요구사항·확인 기록은 req 명령을 사용하므로 DEV-01에서 제외하고, 일반 커밋은 commit plan/apply를 사용하므로 DEV-03에서 제외한다. 일반 설계 문서 편집과 미지원 기능의 예외, 기존 Claude 개발 복사본 동기화에 대한 DEV-04는 필요한 범위에서 유지한다. 전체 Git 이력 검사와 훅이 없다는 이유로 일반 커밋까지 계속 우회하지 않는다.

공용 스킬의 형식 검사, 로컬 복사본 동기화·보존 테스트 5개, 배포 패키지의 외부 설치 검증을 통과했다. 도입 전 note 18개의 내용과 기존 기준선이 보존된 것을 확인했다. 지침 파일이 없는 별도 프로젝트에서 새 에이전트가 자율적으로 도입하는 종단 시험과 문서 없는 프로젝트의 요구사항 도출은 이번 자체 적용만으로 검증됐다고 보지 않는다. 갱신한 스킬은 다음 배포에 포함할 소스이며 이미 게시된 0.2.1 패키지를 변경하지 않는다.

## 첫 기록 단위

제품 원칙 2개, 에이전트 도입 3개, 기록 정책 3개, Git 연결 2개, 조회 2개를 자동 확정했다. 요구사항 GUI와 전체 Git 이력 검사 2개는 초안으로 남겼다. 실제 내용과 ID는 `.tryce/spec/<영역>/tryce.json`에서 확인한다. 기존 구현 전체에 완료를 선언하거나 과거 커밋에 참조를 붙이지 않는다.

2026-09-14 사용자는 이름을 Tryce로 유지하고 준비한 변경을 커밋하도록 요청했다. 별도 저장소로 옮기거나 Git 이력을 초기화하지 않고, 현재 이력 위에서 자동모드와 요구사항 기록을 적용한다. 이 도입 기록은 `commit plan/apply`의 일반 기록 커밋으로 남기며, 이후 구현 커밋부터 해당 작업의 실제 확정 요구사항을 연결한다.

## 2026-09-14 로컬 demo 시나리오

사용자가 위임한 `.tmp/demo` 시험을 같은 Codex 세션에서 수행했다. 설치된 CLI는 npm의 0.2.1이며 실행 파일 해시는 개발 환경에 지정한 빌드와 같았다. demo 지침에는 0.1.0이 남아 있었고 스킬도 note 전용 구버전이었다. 기존 지침·스킬 사본을 로컬 시험 자료에 보존한 뒤 지침을 정정하고, 공용 스킬의 `64125cb` 버전을 명시적으로 적용했다. CLI의 install/sync가 기존 원본을 자동 업그레이드했다고 보지 않는다. Claude 복사본은 demo의 CLI로 sync했다.

기존 empty 기준선과 note 1개의 원문을 보존하며 auto로 전환했다. GUI 없는 메모 목록 함수를 시험 대상으로 만들어 아래 흐름을 확인했다. 모든 요구사항과 확인 답변은 시험 데이터이며 제품 사용자의 실제 승인으로 표시하지 않았다.

| 시나리오 | 관측 결과 |
| :--- | :--- |
| 자동모드에서 완료 여부 필터 요청 | `R-notes-owupkvxpvo`를 auto 확정하고 구현·테스트 후 `2da99c1`에 연결. 사용자 승인 상태는 not-approved |
| 승인모드에서 제목 검색·정렬을 함께 제안 | 두 초안을 묶고 확인 전 소스가 그대로이며 승인 기록이 없는 것을 확인 |
| 검색만 확인하는 시험 답변 | 동일한 검색 원문만 새 묶음으로 확인. `R-notes-bbn3we56ub`만 승인하고 구현 커밋 `caa57e3`에 연결 |
| 정렬 보류 | `R-notes-htwfw4hcrr`는 draft/not-approved. 구현 참조에서 제외하고 입력 순서 보존을 테스트 |
| 다시 조회 | 별도 CLI 프로세스의 req list와 brief에서 자동 확정·시험 승인·미확정 상태를 구분. 과거 auto 결정은 모드 전환 후에도 보존 |

필터·검색 조합, 빈 결과, 잘못된 필터 값, 입력 배열·객체 보존과 원래 순서를 확인하는 Node 테스트 5개가 통과했다. demo의 첫 준비 커밋은 `f223900`이고 세 커밋 모두 commit plan/apply로 작성했다. 최종 작업 폴더는 깨끗하며 demo는 approval 모드로 남겨 두었다. 구현과 테스트 결과가 있다는 이유로 요구사항의 not-assessed/not-run을 변경하지 않았다.

로컬 시험 자료는 `.tmp/demo-e2e/`에 있다. CLI 호출 로그, 확인 전 상태, `Codex test operator`의 모의 답변, 최종 조회와 결과를 보존했다. `.tmp/`는 추적하지 않으므로 이 문서에 재현할 시나리오와 결과를 남긴다.

이번 결과는 같은 에이전트가 준비한 시나리오의 실행 검증이다. 독립된 새 에이전트의 스킬 발견·자율 선택, 실제 사람과의 승인 대화, 수정 없는 공개 패키지만으로 최신 스킬을 도입하는 흐름은 검증하지 않았다. CLI 업데이트와 기존 프로젝트 스킬 갱신은 별도 작업이라는 안내가 다음 도입 개선의 확인 대상이다.
