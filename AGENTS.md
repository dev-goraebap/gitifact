# tryce 개발 지침

이 저장소는 tryce를 개발하면서 프로젝트 자체에도 적용한다. 제품의 기록 원칙을 따르되, 아직 구현되지 않은 기능은 아래 대체 절차로 수행한다. 이 예외는 tryce 개발 저장소에만 적용하며 제품의 기본 동작으로 확장하지 않는다.

현재 이 프로젝트는 사용자가 선택한 자동모드(`auto`)이며 기록 형식은 `workflow-1`이다. [.tryce/config.json](.tryce/config.json)의 도입 기준선을 보존하고, [프로토타입 기록](docs/specs/prototype-notes.md)을 CLI로 작성한다. 검증·지정한 빌드와 실행 범위는 [개발 환경](docs/development.md)을 따른다. 설정을 직접 덮어쓰거나 재초기화로 기준선·모드를 바꾸지 않는다. 요구사항·확인·커밋 기능은 지정된 CLI 0.3.0으로 사용한다. 요구사항은 `.tryce/spec/`에 CLI로 기록하고, 커밋은 `commit plan/apply`를 사용한다. 도입 근거와 등록 범위는 [자체 도입 기록](docs/adoption.md)을 따른다. 모드를 임의로 바꾸지 않는다. 전체 이력 검사와 훅은 미지원이다.

개발 작업에는 [tryce-workflow](.agents/skills/tryce-workflow/SKILL.md)를 읽고 적용한다. 원본만 커밋하고 이 저장소의 기존 Claude 복사본은 `pnpm skills:sync`로 로컬 생성한다. 복사본의 직접 수정은 원본과 비교해 보존한다. 새 프로젝트에는 [CLI 스킬 배포](docs/specs/skill-distribution.md)를 사용한다. DEV-04는 검증한 install·sync·로컬 복사본 remove 범위에서 축소한다. 이전 개발 매니페스트 연결, 미지원 원본 upgrade·전체 eject·훅에는 유지한다.

빌드가 준비돼 있으면 작업 시작 시 `pnpm cli brief --format text`로 설정·Git 상태·기록·문서 위치를 확인한다. 생략 수와 본문 발췌 여부를 확인하고 필요한 범위는 `brief --all`, `note list`, `note show ID`와 실제 문서 원문으로 보완한다. 부분 실패는 stderr의 report와 자료별 오류를 확인한다. brief는 원문 지침 읽기나 요구사항 검사를 대신하지 않는다. 이 관측 범위는 검증된 brief를 사용하므로 DEV-02의 수동 브리핑 예외에서 제외하고, 요구사항 검사·미결 질문·이력 등 미지원 범위는 유지한다.

중요한 발견·제약·기각 이유는 `note add`로 기록하고, 수정은 새 기록의 `--ref` 또는 `--supersedes`로 연결한다. `.tryce/notes/`의 JSON을 직접 덮어쓰지 않는다. 이 범위는 검증된 note 명령을 사용하므로 DEV-01의 직접 기록 예외에서 제외한다. 요구사항·수정본·확인 기록도 검증된 req 명령을 사용하므로 DEV-01에서 제외한다. 일반 설계 문서와 미지원 기록 형식에는 기존 예외를 유지한다.

## 작업을 시작할 때

1. `git status`와 현재 브랜치·작업 경로를 확인한다. 기존 변경과 다른 에이전트의 작업을 보존한다.
2. [제품·설계 기준](docs/bref.md)을 읽고 해당 작업의 확정·합의·보류 상태를 확인한다. 이후 작성된 관련 명세도 읽는다.
   코드 구조와 기술 선택은 [아키텍처 기준](docs/architecture/README.md)을 따른다. CLI 또는 브라우저를 작업할 때는 해당 상세 문서도 읽는다.
3. 실제 파일과 실행 가능한 명령을 확인한다. README나 설계 문서에 등장한다는 이유만으로 기능이 구현됐다고 가정하지 않는다.
   실행 방법과 검증 범위는 [개발 환경](docs/development.md)을 확인한다. 현재 통합 검증 명령은 `pnpm check`다.
4. 이번 작업의 범위, 관련 문서, 검증 방법을 정하고 진행한다.

`.tmp/`는 로컬 조사·논의 자료다. 새 체크아웃에 없어도 작업을 시작할 수 있어야 한다. 구현에 필요한 결정은 `docs/`의 관련 문서에 남긴다. 새 문서는 [제품·설계 기준](docs/bref.md)에서 찾을 수 있도록 연결한다.

## 항상 지킬 원칙

- 사용자와 다른 에이전트의 변경을 덮어쓰거나 임의로 되돌리지 않는다. 파일을 수정하기 전에 최신 내용을 확인한다.
- 변경 이유와 제외한 접근 중 이후 작업에 영향을 주는 내용을 관련 문서에 남긴다. 사소한 편집마다 별도 결정 문서를 만들지는 않는다.
- 구현 중 요구사항이 바뀌면 관련 문서와 코드를 함께 맞춘다. 커밋할 때도 같은 변경으로 묶는다.
- 확정된 규약을 임의로 바꾸지 않는다. 보류 사항은 기본값으로 확정된 것처럼 구현하지 않는다.
- 사용자 결정이 필요한 제품 의미·영속 규약과 되돌리기 쉬운 구현 선택을 구분한다. 기존 결정 안에서의 구현·검증·수정은 진행하고, 결정에 의존하지 않는 작업도 계속한다.
- 참조 커밋, 작업 완료 표시, 테스트 결과를 각각 사실로 기록한다. 하나가 있다는 이유만으로 다른 것까지 완료됐다고 판단하지 않는다.
- 실제로 실행한 검증과 실행하지 못한 검증을 구분한다. 기능이 없거나 입력이 부족하면 미실행이라고 보고한다.
- 외부 스킬·플러그인·사용자 설정은 요청된 범위 밖에서 수정하지 않는다. 이름이나 접두어만으로 tryce 소유 파일이라고 판단하지 않는다.

브랜치와 Git worktree 사용 방식은 강제하지 않는다. 현재 체크아웃을 기준으로 작업하며, 공유되는 설정이나 훅을 바꿀 때는 다른 worktree에 미치는 영향을 확인한다. 다른 에이전트가 별도로 맡은 작업을 중복 수행하지 않는다.

## 개발 초기의 대체 절차

아래 조건에 해당하면 매번 별도 승인을 받지 않고 대체 절차를 사용할 수 있다. 적용한 예외와 이유는 작업 결과에 짧게 남긴다. 권한·사용자 변경 보존·검증 보고 원칙은 예외 대상이 아니다.

| 예외 | 적용 조건 | 대체 절차 | 종료 조건 |
| :--- | :--- | :--- | :--- |
| DEV-01 문서 직접 작성 | 필요한 요구사항·설계·기록 명령이 없거나 아직 프로젝트 사용 대상으로 검증되지 않음 | 관련 Markdown과 메타데이터를 직접 작성한다. 확정된 형식이 있으면 준수하고 변경 이유를 남긴다. 아직 정하지 않은 스키마를 영속 규약으로 만들지 않는다. | 해당 쓰기 명령과 형식 검증을 시험하고 프로젝트 사용 대상으로 지정함 |
| DEV-02 수동 브리핑·검사 | brief/check가 없거나 이번 검사 범위를 지원하지 않음 | Git, 관련 문서, 독립적인 검증 도구로 상태를 확인한다. 검사한 범위와 누락된 범위를 보고한다. | 필요한 범위의 조회·검사가 구현되고 지원 형식에서 검증됨 |
| DEV-03 기본 Git으로 커밋 | 지원하지 않는 커밋 작업 또는 명시적인 기본 Git 요청에 한정. 일반 기록·구현 커밋에는 적용하지 않음 | 기본 Git 명령으로 staged 변경을 검토하고 커밋한다. 실제 요구사항을 참조할 때는 확정된 트레일러 형식을 따른다. 참조할 요구사항이나 적용된 형식이 없으면 허구의 ID·승인·버전을 만들지 않고 일반 커밋으로 기록한다. | 0.2.1의 commit plan/apply를 프로젝트에 적용함. 미지원 전체 이력 검사·훅은 별도 범위 |
| DEV-04 스킬·연동 직접 준비 | init/sync/eject가 없거나 대상 연동을 지원하지 않음 | 필요한 연동 파일만 직접 작성한다. 경로·출처·변경 이유와 제거 범위를 관련 문서에 남기고 기존 파일을 보존한다. 보류 중인 배포 방식을 제품 기본값으로 확정하지 않는다. | 대상 연동의 설치·갱신·제거와 사용자 변경 보존이 검증됨 |

대체 절차로 기록한 내용은 프로젝트 기록이다. 정식 기능을 도입할 때 누락되거나 폐기되지 않도록 전환 계획에 포함한다. 명세가 정해지기 전에 가짜 승인이나 형식 버전으로 완성된 이력을 꾸미지 않는다.

## 예외를 종료하거나 추가할 때

명령이 존재한다는 사실만으로 예외가 끝나지는 않는다. 관련 정상 동작과 실패·복구 경로를 검증하고, 프로젝트에서 사용할 실행 방법을 문서화한 뒤 해당 예외의 상태를 갱신한다. 기존에 승인된 범위의 기능 도입은 반복해서 허락받을 필요가 없다.

부분 지원이면 예외를 해당 미지원 범위로 좁힌다. 종료 기록에는 지원 범위, 검증 근거, 도입 버전 또는 커밋을 남긴다. 새 세션이 같은 우회를 반복하지 않도록 이 파일의 표에도 종료 또는 축소 사실을 반영한다.

새 예외가 필요하면 적용 조건, 대체 절차, 보존할 정보, 종료 조건을 구체적으로 적는다. 테스트 실패나 구현의 불편함만을 이유로 형식 검증을 끄거나 `--no-verify`를 상시 사용하지 않는다. 이미 활성화된 검증이 실패하면 원인을 확인하고 수정한다.

## 개발 버전과 프로젝트 사용 버전

지금 수정하는 CLI와 이 저장소의 기록을 관리하는 CLI는 역할을 구분한다. 프로젝트 사용 버전은 임시 저장소에서 관련 기능을 검증한 뒤 지정하고, 실행 방법과 지원 형식을 `docs/`에 기록한다. 로컬 빌드도 검증과 지정 과정을 거치면 사용할 수 있다.

지정된 버전이 없으면 개발 초기 대체 절차를 사용한다. PATH에 `tryce`가 있다는 이유로 이 프로젝트의 쓰기 도구로 신뢰하거나, 새 빌드를 자동으로 프로젝트 사용 버전으로 바꾸지 않는다.

기록·동기화·마이그레이션·제거 기능은 임시 저장소에서 먼저 시험한다. 실제 프로젝트의 이력이나 사용자 설정을 실패 사례의 실험 대상으로 사용하지 않는다. 임시 산출물은 Git 추적에서 제외하고 시험 대상을 현재 프로젝트와 구분한다.

## 검증과 변경 마무리

변경에 맞는 검증을 수행한다. 코드의 동작 변경은 의미 있는 테스트로 확인하고, 문서만 바뀐 경우에는 내용 일관성·링크·형식을 확인한다. 실제 패키지와 스크립트가 생기기 전에는 빌드·테스트 명령을 추측해서 안내하지 않는다.

tryce 자신의 check 결과만으로 구현이 맞다고 판단하지 않는다. 파서·상태 전이·부분 staging·형식 호환성·마이그레이션은 예상 결과가 고정된 자료와 별도의 테스트로 확인한다. 과거 읽기 지원과 쓰기 거부, 실패 후 복구도 검증한다.

작업 결과에는 변경 내용, 이유, 검증 결과, 남은 제한을 간단히 보고한다. 적용한 개발 예외가 있으면 ID와 이유를 덧붙인다. 커밋·푸시는 사용자가 요청하거나 현재 작업에 부여한 권한 범위에서 수행한다.

## 문체

문서는 짧고 담백하게 작성한다. 목적과 동작을 먼저 설명하고, 필요한 근거와 대가를 덧붙인다. 과장된 확신, 반복적인 강조, 불필요한 구호를 피한다. 확정된 내용과 제안·미결 사항을 구분하고, 구현되지 않은 기능은 계획으로 표시한다.
